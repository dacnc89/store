<div class="cart-page">
  You have <%= pluralize(current_cart.order_items.count, 'Product') %> in your Cart<br>

<% if current_cart.order_items == nil %>
  @cart,order_items == nil 
<% else %>
  @cart.order_item not nil 
<% end %>







<% @order_items.each do |item| %>
  show item here
  <% @product = Product.find(item.id) %>
quantity:  <%= item.quantity %>
id:  <%= item.id %>
price: <%= item.quantity*@product.price %>
<% end %>



</div>
